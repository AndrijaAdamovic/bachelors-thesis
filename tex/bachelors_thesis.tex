\documentclass[12pt,a4paper]{report}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[english]{babel}
\usepackage[a4paper,left=25mm,right=25mm,top=25mm,bottom=25mm,headheight=15pt]{geometry}
\usepackage{lmodern}
\usepackage{amsmath}
\usepackage{amsthm}
\usepackage{amssymb}
\usepackage{array}
\usepackage{booktabs}
\usepackage{caption}
\usepackage{fancyhdr}
\usepackage{graphicx}
\usepackage{pdfpages}
\usepackage{xcolor}
\usepackage[
    pdfpagelabels,
    plainpages=false,
    colorlinks=true,
    linkcolor=blue,
    citecolor=blue,
    urlcolor=blue
]{hyperref}
\usepackage{titlesec}
\usepackage{tocloft}
\usepackage{theoremref}
\usepackage{arydshln}
\usepackage{tikz}
\usetikzlibrary{calc, patterns, decorations.pathmorphing, arrows.meta, positioning}
\usepackage{subcaption}
\usepackage{algorithm}
\usepackage{algpseudocode}

\hypersetup{
    colorlinks=true,
    linkcolor=blue,
    citecolor=blue,
    urlcolor=blue,
    pdfborder={0 0 0},
    linktoc=all,
    pageanchor=false
}

\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt}
\setlength{\cftbeforechapskip}{10pt}
\setlength{\cftbeforesecskip}{5pt}
\setlength{\cftbeforefigskip}{5pt}
\setlength{\cftbeforetabskip}{5pt}

\captionsetup{
    font=small,
    labelfont=bf,
    labelsep=colon
}

\titleformat{\chapter}[hang]
    {\normalfont\Large\bfseries}
    {\thechapter}
    {1em}
    {}
\titleformat{name=\chapter,numberless}[hang]
    {\normalfont\Large\bfseries}
    {}
    {0pt}
    {}
\titleformat{\section}[hang]
    {\normalfont\large\bfseries}
    {\thesection}
    {1em}
    {}
\titlespacing*{\chapter}{0pt}{0pt}{18pt}
\titlespacing*{\section}{0pt}{14pt}{8pt}

\newcommand{\university}{University of Zagreb}
\newcommand{\faculty}{Faculty of Mechanical Engineering and Naval Architecture}
\newcommand{\thesistype}{Bachelor's Thesis}
\newcommand{\runningthesistype}{Bachelor's Thesis}
\newcommand{\authorname}{Andrija Adamovi\'{c}}
\newcommand{\mentorone}{Prof.\ dr.\ sc.\ Andrej Joki\'{c}}
\newcommand{\mentortwo}{}
\newcommand{\placeyear}{Zagreb, 2026}
\newcommand{\thesistitle}{Thesis Title}
\newcommand{\thesistitlecroatian}{Naslov rada}
\newcommand{\studentid}{}
\newcommand{\keywords}{keyword one; keyword two; keyword three}
\newcommand{\kljucnerijeci}{klju\v{c}na rije\v{c} jedan; klju\v{c}na rije\v{c} dva}


\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small \authorname}
\fancyhead[R]{\small \runningthesistype}
\fancyfoot[L]{\small\textit{\faculty{}}}
\fancyfoot[R]{\small\textit{\thepage}}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0.4pt}

\fancypagestyle{plain}{
    \fancyhf{}
    \fancyhead[L]{\small \authorname}
    \fancyhead[R]{\small \runningthesistype}
    \fancyfoot[L]{\small\textit{\faculty{}}}
    \fancyfoot[R]{\small\textit{\thepage}}
    \renewcommand{\headrulewidth}{0.4pt}
    \renewcommand{\footrulewidth}{0.4pt}
}

\newcommand{\frontmatterchapter}[1]{%
    \chapter*{#1}%
    \addcontentsline{toc}{chapter}{#1}%
}

\newcommand{\clearemptypage}{%
    \clearpage
    \thispagestyle{empty}%
}

\newtheorem{definition}{Definition}
\newtheorem{theorem}{Theorem}

\newcommand{\norm}[1]{\left\lVert #1 \right\rVert}

\begin{document}

\begin{titlepage}
    \centering
    \thispagestyle{empty}

    {\large\scshape \university\par}
    \vspace{3mm}
    {\large\scshape \faculty\par}

    \vspace*{80mm}
    {\Huge\scshape \MakeUppercase{\thesistype}\par}

    \vspace{38mm}
    \begin{flushright}
        {\Large \authorname\par}
    \end{flushright}

    \vfill
    {\large\scshape \placeyear\par}
\end{titlepage}

\begin{titlepage}
    \centering
    \thispagestyle{empty}

    {\Large\scshape \university\par}
    \vspace{3mm}
    {\Large\scshape \faculty\par}

    \vspace*{80mm}
    {\Huge\scshape \MakeUppercase{\thesistype}\par}

    \vspace{38mm}
    \noindent
    \begin{minipage}[t]{0.52\textwidth}
        \raggedright{}
        {\Large Mentor:\par}
        \vspace{9mm}
        {\Large \mentorone\par}
        \ifx\mentortwo\empty\else
            \vspace{3mm}
            {\Large \mentortwo\par}
        \fi
    \end{minipage}
    \hfill
    \begin{minipage}[t]{0.34\textwidth}
        \raggedleft{}
        {\Large Student:\par}
        \vspace{9mm}
        {\Large \authorname\par}
    \end{minipage}

    \vfill
    {\large\scshape \placeyear\par}
\end{titlepage}

\clearemptypage{}
\vspace*{\fill}
\noindent I hereby declare that I have written this thesis independently, using the knowledge
acquired during my studies and the literature cited in the references.

\vspace{8mm}
\noindent TODO\@: Zahvale


\vspace{10mm}
\begin{flushright}
    \authorname{}
\end{flushright}
\vspace*{\fill}
\clearpage

\includepdf[
    pages=1,
    pagecommand={\thispagestyle{empty}},
    width={\paperwidth}
]{figures/assignment.pdf}

\pagenumbering{roman}
\setcounter{page}{1}
\pagestyle{fancy}
\hypersetup{pageanchor=true}

\phantomsection{}
\addcontentsline{toc}{chapter}{CONTENTS}
\tableofcontents
\clearpage

\phantomsection{}
\addcontentsline{toc}{chapter}{LIST OF FIGURES}
\listoffigures
\clearpage

\phantomsection{}
\addcontentsline{toc}{chapter}{LIST OF TABLES}
\listoftables
\clearpage

\frontmatterchapter{LIST OF SYMBOLS}
\begin{tabular}{@{}p{0.18\textwidth}p{0.58\textwidth}p{0.16\textwidth}@{}}
\toprule
Symbol & Description & Unit \\
\midrule
$x$ & Example variable & / \\
\bottomrule
\end{tabular}
\clearpage

\frontmatterchapter{LIST OF ABBREVIATIONS}
\begin{tabular}{@{}p{0.22\textwidth}p{0.72\textwidth}@{}}
\toprule
Abbreviation & Description \\
\midrule
LTI & Linear time-invariant \\
\bottomrule
\end{tabular}
\clearpage

\frontmatterchapter{SA\v{Z}ETAK}
Ovdje upi\v{s}ite sa\v{z}etak rada na hrvatskom jeziku.

\vspace{8mm}
\noindent\textbf{KLJU\v{C}NE RIJE\v{C}I:} \kljucnerijeci{}
\clearpage

\frontmatterchapter{ABSTRACT}
Write the thesis abstract in English here.

\vspace{8mm}
\noindent\textbf{Keywords:} \keywords{}
\clearpage

\pagenumbering{arabic}
\setcounter{page}{1}

\chapter{Introduction}
Classical control systems almost always use a system model to synthesise a control algorithm.

\section{Motivation}
Describe the background and motivation for the thesis.

\section{Literature Overview}

\chapter{The Fundamental Lemma}

\section{Theoretical Background}
The goal of this section is to provide introduction to mathematical concepts needed for understanding
the rest of the thesis. We will lay out some famous results from linear control systems theory.
\subsection{LTI Dynamical Systems}
Although Willems' Fundamental Lemma stems from the behavioural approach to sytems theory, in this thesis it is formulated using the 
classical state-space LTI system formulation. This formulation is simpler, yet sufficient for the further chapters of the thesis. 

\begin{definition} 
    A discrete-time linear time-invariant (LTI) system is a dynamical system whose dynamics are described by:
    \begin{subequations}
        \begin{align}
            x(t+1) &= Ax(t) + Bu(t) \\
            y(t) &= Cx(t) + Du(t)
        \end{align}
        \label{eq:lti}
    \end{subequations}
    Where \(t \in \mathbb{Z}_{\ge 0}\) is the time axis, \(x(t) \in \mathbb{R}^{n}\) is the state, \(u(t) \in \mathbb{R}^{m}\) is the input
    and \(y(t) \in \mathbb{R}^{p}\) is the output of the system. \\
    The true state space dimension is $n$ and system matrices \(A \in M_n(\mathbb{R})\), \(B \in M_{n \times m}(\mathbb{R})\), 
    \(C \in M_{p \times n}(\mathbb{R})\), \(D \in M_{p \times m}(\mathbb{R})\).
\end{definition}

In our data-based approach, those matrices (i.e. the system model) are  generally unknown. However, an upper bound on state space dimension is
given $N \geq n$. The concept of a system trajectory is defined now.

\begin{definition}
    A sequence of ordered triples \(\left( u(t), x(t), y(t) \right)_{t=0}^{\infty}\) is called an input-state-output trajectory
    if 

    \[ \begin{bmatrix} x(t+1) \\ y(t) \end{bmatrix} = \begin{bmatrix} A & B \\ C & D  \end{bmatrix} 
    \begin{bmatrix} x(t) \\ u(t)\end{bmatrix}\]

\end{definition}

We can be deduce that, from the linearity of \eqref{eq:lti}, any linear combination of input-state-ouput 
trajectories is itself an input-state-output trajectory i.e. the set of all such trajectories spans a vector space over $\mathbb{R}$.
That space is called the \textit{behaviour} of the system.
 
Additionally, from time invariance \eqref{eq:lti} it can be deduced that:

\[\left( u(t + \tau), x(t + \tau), y(t + \tau) \right)_{t=0}^{\infty}\]

is an input-state-output trajectory for all \(\tau \in \mathbb{N}\)
\begin{definition}
    A sequence of ordered pairs \(\left( u(t), y(t) \right)_{t=0}^{\infty}\) is called an input-output trajectory if 
there exists a sequence of states \(x: \mathbb{Z}_{\ge 0} \to \mathbb{R}^{n}\) such that \(\left( u(t), x(t), y(t) \right)_{t=0}^{\infty}\) 
is an input-state-output trajectory.
\end{definition}

In practice we are limited by finite computer memory so it is not possible work with infinite sequences of data like the trajectories defined above, so we
need to take a snippet of the trajectory relevant to us. Motivated by that we state the next definition.

\clearpage
\begin{definition}
    Let \(i,j \in\ \mathbb{Z}_{\ge 0}\ \) such that \(i \leq j\). Given an input-state-output trajectory  \(\left( u(t), x(t), y(t) \right)_{t=0}^{\infty}\),
    the sequence  \(\left( u(t), x(t), y(t) \right)_{t=i}^{j}\) is called a \textit{restricted input-state-output trajectory}. Restricted 
    input-output trajectories are defined in the same way.
\end{definition}

\subsection{The Concepts of Contollability and Observability}

In this section the concepts of controllability and observability will be defined. Since, we defined the LTI system \eqref{eq:lti} 
classically the same will follow for the next definitions. However, although the classical and behavioral notions of controllability 
and observability are closely related, they are not identified unconditionally. Their equivalence depends on how the behavior is represented. 
In particular, when considering input-output behavior, equivalence with the classical state-space definitions typically requires a minimal realization; 
for the full input-state-output behavior, the correspondence is more direct. We will not bother with those comparisons between approaches, more on that 
topic can be found at (TODO: link za clanak/knjigu).

\begin{definition}
    A discrete LTI system \eqref{eq:lti} is called controllable if for any initial state $x(0)$ and any desired state $x_f$, there exists
    a finite-length sequence of inputs 
    \[u(0), u(1), ..., u(N-1)\]
    such that the system reaches \(x(N) = x_f\). Otherwise, the system is said to be uncontrollable.
\end{definition}

In lays terms, it is telling that any state of the system can be reached in finite time by applying finitely many inputs. 

We will now state the famous result for testing system controllability stated and proven by Rudolf. E. Kalman. The pair $(A, B)$ is the pair of matrices from \eqref{eq:lti} and n is
the true state space dimension of the system.

\begin{theorem}(Kalman controllability theorem)
    The pair $(A, B)$ is controllable if and only if the controllability block matrix
    \[ \mathcal{C} = \begin{bmatrix} B & AB & A^{2}B & \cdots & A^{n-1}B\end{bmatrix}\]
    is of full rank.
    \label{thm:ctrb}
\end{theorem}

Now the concept of observability is defined.

\begin{definition}
    A discrete LTI system \eqref{eq:lti} is called observable at $t = 0$ if the initial state x(0) can be uniquely reconstructed from a known 
    restricted input-output trajectory. Otherwise, the system is said to be unobservable.
\end{definition}

Using time-invariance of \eqref{eq:lti} it can be shown that if the system is observable at $t = 0$ then for every  \(t \in \mathbb{Z}_{\ge 0}\) the state $x(t)$ is reconstructible.
This result is important to control theory when it comes to state estimation (for example Kalman filter) because it gives a necessary condition for exact state estimation.

Now the test for determining observability will be stated.

\begin{theorem}(Kalman observability theorem)
    The pair $(C, A)$ is observable if and only if the observability block matrix
    \[ \mathcal{O} = \begin{bmatrix} C \\ CA \\ CA^{2} \\ \vdots \\ CA^{n-1}\end{bmatrix}\]
    is of full rank.
    \label{thm:obsv}
\end{theorem}

The Kalman criteria for controllability/observability were stated but, there are more different equivalent
statements of which the most used ones are the Popov-Belevitch-Hautus (PBH) tests.

From Theorems~\ref{thm:ctrb} and~\ref{thm:obsv}, a duality between observability and controllability matrices can be seen and
it is not hard to show that using matrix operations we have:
\begin{align*}
    (A, B) \ \mbox{is controllable} &\iff (B^T, A^T) \ \mbox{is observable}.
    \intertext{Similarly,}
    (C, A) \ \mbox{is observable} &\iff (A^T, C^T) \ \mbox{is controllable}.
\end{align*}

\subsection{Persistency of Excitation}
For the Willems' Fundamental Lemma to hold we need a so-called persistently exciting input. By that
it means a rich and diverse enough input such that all modes of the system are expressed in the output.

\subsection*{Hankel Matrices}
For a sequence of vectors from time $t = i$ to $t = j$ we use the following vector notation:

\[s_{[i, j]} = \begin{bmatrix} s(i) \\ s(i+1) \\ \vdots \\ s(j) \end{bmatrix}\]

Where instead of vector $s$ we will use inputs($u$), states($x$) and outputs($y$).

Hankel matrix is a matrix whose entries are constant on the anti-diagonals, contrast to
Toeplitz matrix whose entries are constant on the diagonals. Informally, Hankel matrix is
a mirror image of a Toeplitz matrix.


\begin{definition}
    Given an $T \in \mathbb{N}$, lets consider a restricted input-output trajectory \((u_{[0, T-1]},\allowbreak y_{[0, T-1]})\) of \eqref{eq:lti}. Hankel matrices
    of depth $L \in \{1, ..., T\}$ of these inputs and outputs are given by:

    \[H_{L}(u_{[0, T-1]}) = 
    \begin{bmatrix}
    u(0) & u(1) & \cdots & u(T-L) \\
    u(1) & u(2) & \cdots & u(T-L+1)\\
    \vdots & \vdots &  & \vdots \\
    u(L-1) & u(L) & \cdots & u(T-1)
    \end{bmatrix}\]

    \[H_{L}(y_{[0, T-1]}) = 
    \begin{bmatrix}
    y(0) & y(1) & \cdots & y(T-L) \\
    y(1) & y(2) & \cdots & y(T-L+1)\\
    \vdots & \vdots &  & \vdots \\
    y(L-1) & y(L) & \cdots & y(T-1)
    \end{bmatrix}\]

    \label{def:hankel}
\end{definition}

\clearpage

Generally, we stack input and output Hankel matrices to obtain a single block Hankel matrix used for computation:
\[\begin{bmatrix} H_{L}(u_{[0, T-1]}) \\ H_{L}(y_{[0, T-1]}) \end{bmatrix} \in M_{(m+p)L \times (T-L+1)}(\mathbb{R}) \]

It is now obvious that every column of the matrices from  \eqref{def:hankel} is a restricted input or output trajectory, depending on 
which matrix is chosen.

Now a precise definition of persistently exciting input is given.

\begin{definition}
    Given an sequence of inputs $u_{[0, T-1]}$ and \(k \in \{1, ..., T\}\). We say that
    the given sequence of inputs is persistently exciting of order k if the Hankel matrix 
    $H_{k}(u_{[0, T-1]})$ is of full row rank.
\end{definition}

We have layed out all the necessary definitions and theorems needed for the statement of the main topic of this 
thesis, the Lemma.

\section{The Lemma}

The main result of the Lemma is that every restricted input-output trajectory of length $L$ of the system can be obtained by linear combination
of finitely many trajectories which span the whole trajectory space i.e. the behaviour.

\begin{theorem} (Willems' Fundamental Lemma) Given a LTI system \eqref{eq:lti}, assume the pair $(A, B)$ is controllable and let \((u_{[0, T-1]}, y_{[0, T-1]}), \ T \in \mathbb{N}\)
be it's restricted input-output trajectory. Let \( L \in \{1, ..., T\}\), if the input $u_{[0, T-1]}$ is persistently exciting of order $N + L$ then:
\((\bar{u}_{[0, L-1]}, \bar{y}_{[0, L-1]})\) is a restricted input-output trajectory of \eqref{eq:lti} if and only if
\[\begin{bmatrix} \bar{u}_{[0, L-1]} \\ \bar{y}_{[0, L-1]} \end{bmatrix} = \begin{bmatrix} H_{L}(u_{[0, T-1]}) \\ H_{L}(y_{[0, T-1]})\end{bmatrix}g \]
for some $g \in \mathbb{R}^{T-L+1}.$
\end{theorem}

\begin{proof}
    Solving the difference equations of \eqref{eq:lti} we obtain the following result:
    \begin{equation}
        y(t) = CA^{t}x(0) + \left(\sum_{k=0}^{t-1}CA^{t - k - 1}Bu(k)\right) + Du(t)
    \end{equation}
    where $x(0) \in \mathbb{R}^n$ is the initial state. Generally, this could be adapted to any initial state $x_{ini} = x(t_{ini})$ using the time-invariance of the system. Now for the input-output
    trajectory \((\bar{u}_{[0, L-1]}, \bar{y}_{[0, L-1]})\) we can write the result from above in matrix notation like this:
    \[ 
    \begin{bmatrix} \bar{y}(0) \\ \bar{y}(1) \\ \bar{y}(2) \\ \vdots \\ \bar{y}(L-1)\end{bmatrix} = 
    \underbrace{\begin{bmatrix} C \\ CA \\ CA^2 \\ \vdots \\ CA^{L-1} \end{bmatrix}}_\text{$\Omega_L$}x_{ini} +
    \underbrace{\begin{bmatrix} D \\ CB & D \\ CAB & CB & D \\ \vdots & \ddots & \ddots & \ddots \\ CA^{L-2}B & \dots & \dots & \dots & D\end{bmatrix}}_\text{$\Theta_L$}
    \begin{bmatrix} \bar{u}(0) \\ \bar{u}(1) \\ \bar{u}(2) \\ \vdots \\ \bar{u}(L-1)\end{bmatrix}
    \]
    \clearpage
    Matrix  $\Omega_L$ is the classical observability matrix and  $\Theta_L$ is the block Toeplitz matrix of, so called, Markov parameters. Now we can reformulate the last expression
    like this:

    \begin{equation}
        \begin{bmatrix} \bar{u}_{[0, L-1]} \\ \bar{y}_{[0, L-1]}\end{bmatrix} = \begin{bmatrix}0 & I \\ \Omega_L & \Theta_L \end{bmatrix} \begin{bmatrix}x_{ini} \\ \bar{u}_{[0, L-1]} \end{bmatrix}
        \label{eq:io}
    \end{equation}

    Since on the left side is a general input-output trajectory of length $L$ (assuming time-invariance), we conclude that the space of all such trajectories is spanned by the columns of the 
    \(M := \begin{bmatrix}0 & I \\ \Omega_L & \Theta_L \end{bmatrix} \) i.e. that space is the image of that matrix. By setting the RHS of \eqref{eq:io} equal to 0 and applying 
    the rank-nullity theorem (R-N) we get:
    \[        \bar{u}_{[0, L-1]} = 0, \ \Omega_L x_{ini} = 0; \]
    \begin{align*}
        \implies \dim(\ker M) &= \dim(\ker \Omega_L) \underbrace{=}_\text{R-N} n - \mbox{rank}\ \Omega_L \\
        \underbrace{\implies}_\text{R-N} \mbox{rank} \ M &= (n + mL) - \dim(\ker M) = \\ 
        &= (n + mL) - (n - \mbox{rank}\ \Omega_L) = \\
        &= \mbox{rank}\ \Omega_L + mL
    \end{align*}
    % \[ \dim(\ker M)\]
    % \[\mbox{rank} \begin{bmatrix}0 & I \\ \Omega_L & \Theta_L \end{bmatrix} = dim \ im \begin{bmatrix}0 & I \\ \Omega_L & \Theta_L \end{bmatrix} = \mbox{rank}(\Omega_L) + mL \]
    The columns of the Hankel matrix \(\begin{bmatrix} H_{L}(u_{[0, T-1]}) \\ H_{L}(y_{[0, T-1]}) \end{bmatrix}\) are input-output trajectories so we conclude that:
    \[im\begin{bmatrix} H_{L}(u_{[0, T-1]})\\ H_{L}(y_{[0, T-1]})\end{bmatrix} \subseteq im\begin{bmatrix}0 & I \\ \Omega_L & \Theta_L \end{bmatrix}\]
    To prove the Lemma we need to prove the equality of those spaces. We assume the strict inclusion:
    \[im\begin{bmatrix} H_{L}(u_{[0, T-1]})\\ H_{L}(y_{[0, T-1]})\end{bmatrix} \subsetneq im\begin{bmatrix}0 & I \\ \Omega_L & \Theta_L \end{bmatrix}\]
    The expression \eqref{eq:io} can be used to formulate an analog expression using Hankel matrices like this:
    \[\begin{bmatrix} H_{L}(u_{[0, T-1]}) \\ H_{L}(y_{[0, T-1]}) \end{bmatrix} = \begin{bmatrix}0 & I \\ \Omega_L & \Theta_L \end{bmatrix} \begin{bmatrix} X_{[0, T-L]} \\ H_{L}(u_{[0, T-1]})\end{bmatrix}\]
    Where \(X_{[0, T-L]} := [x(0) \ x(1) \ ... \ x(T-L)]\).
    By our assumption the matrix \(J_L := \begin{bmatrix} X_{[0, T-L]} \\ H_{L}(u_{[0, T-1]})\end{bmatrix}\) must not have full row rank, if it were full row rank than the rank of Hankel matrix on the left would be
    equal to the rank of $J_L$ and thus the image spaces would be equal. That implies that the left kernel of $J_L$ is non-trivial, in other words,
    there exists a non-zero vector \(v = (a^T, b_{0}^T, b_{1}^T, ... , b_{L-1}^T), \ a\in\mathbb{R}^n, \ b_{i} \ \in\mathbb{R}^{mL}\) such that \(vJ_L = 0\). Expanding we get:
    \begin{equation}a^{T}x(t) + \sum_{k=0}^{L-1}b_{k}^Tu(t+k) = 0, \ \ \forall t \in \{0, 1, ..., T-L\} \label{eq:p0}\end{equation} Shifting the above equation by an arbitray $j\in\mathbb{N}$ and solving the difference equations of \eqref{eq:lti} for the shifted state
    (similar to \eqref{eq:io}) we obtain:
    \begin{equation}
        a^{T}A^{j}x(t) +  \sum_{i=0}^{j-1}a^{T}A^{j-i-1}Bu(t+i) + \sum_{k=0}^{L-1}b_{k}^{T}u(t+k+j) = 0
        \label{eq:p1}
    \end{equation}
    We want to remove the state $x(t)$ from the equation, the easiest way to do it is to leverge the Cayley-Hamilton theorem which states that matrix A satisfies it's characteristic polynomial 
    \(p(\lambda) = \lambda^n + \alpha_{n-1}\lambda^{n-1} + ... + \alpha_1\lambda + \alpha_0\);
    \[p(A) = A^n + \alpha_{n-1}A^{n-1} + ... + \alpha_1A + \alpha_0I = 0 \]

    Now, if we take $n+1$ copies of equation \eqref{eq:p1} for each $j = 0, 1, ..., n$, and multiply each one with its corresponding $\alpha_j$ and and sum them all, the terms with $x(t)$ cancel
    as a result of C-H theorem. Now we get the following expression with only the inputs:
    \begin{equation}
        \sum_{l=0}^{n+L-1}\beta_{l}u(t+l) = 0, \ \ \forall t \in \{0, 1, ..., T-L\}
        \label{eq:beta}
    \end{equation}

    The expression for $\beta_l$-s can be computed by equating terms with the sum in which C-H was used and the result is:
    \begin{equation}
        \beta_l := \sum_{j = max(0, l-L+1)}^{min(n, l)}\alpha_{j}b_{l-j}^T + a^T\sum_{j = l+1}^{n}\alpha_{j}A^{j-l-1}B
        \label{eq:beta2}
    \end{equation} 


    Equation \eqref{eq:beta}, by the definition of a Hankel matrix, can be rewritten as:    
    \[\begin{bmatrix} \beta_0 & \beta_1 & \dots & \beta_{n+L-1}\end{bmatrix}H_{n+L}(u_{[0, T-1]}) = 0\]
    Since, $u_{[0, T-1]}$ is persistently exciting of order $N+L$, and $N \geq n$, it is clear that $H_{n+L}(u_{[0, T-1]})$ is of full row rank so we conclude that:
    \[\beta_0 = \beta_1 = ... = \beta_{n+L-1} = 0\]

    From \eqref{eq:beta2} if $l\geq n$ the right sum is empty so \(\beta_n, ..., \beta_{n+L-1}\) depend only on $b_k$-s. Now we get:
    \[\beta_{n+L-1} = \alpha_{n}b_{L-1}^T \underbrace{=}_\text{$\alpha_n = 1$} b_{L-1}^T = 0 \]
    \[\beta_{n+L-2} = b_{L-2}^T + \alpha_{n-1}b_{L-1}^T = b_{L-2}^T = 0 \]
    continuing until $l = n$ we get \(b_0 = b_1 = ... = b_{L-1} = 0\) so the left sum of \eqref{eq:beta2} is 0 and thus for $l \leq n$ the right sum remains. Now:
    \[\beta_{n-1} = \alpha_{n}a^{T}B = a^{T}B = 0\]
    \[\beta_{n-2} = a^{T}(AB + \alpha_{n-1}B) = a^{T}AB + a^{T}\alpha_{n-1}B \implies a^{T}AB = 0\]
    continuing until $l = 0$ we obtain the following result:
    \[a^{T}\underbrace{\begin{bmatrix} B & AB & \dots & A^{n-1}B \end{bmatrix}}_\text{$\mathcal{C}$} = 0\]
    Since (A, B) is controllable then $\mathcal{C}$ is of full rank, and thus \(a = 0 \implies v = 0\). Which contradicts our assumption. Thus:
    \[im\begin{bmatrix} H_{L}(u_{[0, T-1]})\\ H_{L}(y_{[0, T-1]})\end{bmatrix} = im\begin{bmatrix}0 & I \\ \Omega_L & \Theta_L \end{bmatrix}\]
    Which means there exists a $g\in\mathbb{R}^{T-L+1}$ such that for any restricted input-output trajectory \((\bar{u}_{[0, L-1]}, \bar{y}_{[0, L-1]})\) we have:
    \[\begin{bmatrix} \bar{u}_{[0, L-1]} \\ \bar{y}_{[0, L-1]} \end{bmatrix} = \begin{bmatrix} H_{L}(u_{[0, T-1]}) \\ H_{L}(y_{[0, T-1]})\end{bmatrix}g \]
\end{proof}

\clearpage

Technically, the proof above is just the ($\implies$) direction, however, since each column of the stacked Hankel matrix is an restricted input-output trajectory the 
($\impliedby$) direction follows directly from that. Stability of the system is not needed because the Lemma uses finite-horizon trajectories and thus doesn't require boundedness of state as $t \to \infty$, also the observability
is not needed as it can be seen in the formulation and proof. In the proof we have also showed that the matrix:
\[\begin{bmatrix} X_{[0, T-L]} \\ H_{L}(u_{[0, T-1]})\end{bmatrix}\]
is of full rank. Also we have made an indirect assumption regarding row rank for PE. For a matrix to have full row rank, a necessary condition is that the number of columns needs to be atleast equal to the
number of rows, thus PE condition needs that condition fullfilled. When we impose that condition on $H_{N+L}(u_{[0, T-1]})$ we get that:
\begin{equation}
    T - (N + L + 1) \geq m(N+L) \iff T \geq (m+1)(N+L) - 1
    \label{eq:length}
\end{equation}

So for PE the length of initial trajectory $T$ needs to satisfy the condition above for a given $L$. Generally, for a long enough recorded trajectory we can use the Lemma
for a shorter length trajectory. In our Lemma formulation we only used trajectories of length $L$ starting from 0, but it can be shown that the Lemma holds for a general restricted
input output trajectory of length $L$, i.e. for time window of $[i, i + L -1] \ \forall i \in \mathbb{Z}_{\geq 0}$ we have:
\begin{equation}
    \begin{bmatrix} \bar{u}_{[i, i+L-1]} \\ \bar{y}_{[i, i+L-1]} \end{bmatrix} = \begin{bmatrix} H_{L}(u_{[0, T-1]}) \\ H_{L}(y_{[0, T-1]})\end{bmatrix}g
    \label{eq:fl-i}
\end{equation}

Now we will define the lag $\ell$ of the system. Smallest positive integer $k$ s.t. rank \(\Omega_k = \) rank \(\Omega_{k+1}\), where $\Omega_k$ is the observability matrix from 
\eqref{eq:io}, is called the lag of the system, in other words, the smallest $k$ after which the rank of the observability matrix does not change. Clearly, by C-H theorem, $\ell \leq n$. For an observable system that is the minimal trajectory length such that the initial state
$x_{ini}$ can be reconstructed.


\chapter{Lemma - Numerical Examples}

In this chapter we will demonstrate the Fundamental Lemma on concrete linear systems. We will use the system model to gather the necessary data
for the Hankel matrices and then use them to verify for a random trajectory that it lies in the image of the Hankel matrix. For this we use MATLAB because it is
easier to work with its Control System Toolbox for system simulation. TODO:text

\section{Simple SISO System}

Consider a discrete time LTI system \eqref{eq:lti} with state space matrices:
\begin{alignat}{2}
    A &= \begin{bmatrix} 0.5 & 0.1 & 0.1 \\ 0.1 & 0.4 & 0.2 \\ -0.1 & 0.2 & 0.3\end{bmatrix} \ & B &= \begin{bmatrix} -1 \\ 0.1 \\ 0.7\end{bmatrix} \\
    C &= \begin{bmatrix} 0.1 & 2 & 1.5\end{bmatrix}  \ & D &= \begin{bmatrix} 0 \end{bmatrix}
\end{alignat}


Clearly \(n=3, m=p=1\) and the eigenvalues of A are \{0.5834, 0.4657, 0.1509\} which all have their absolute value less then 1 which means the system is stable. Also 
the controllability and observability matrix have a non-zero determinant to the system is both controllable and observable, which means, it is suitable for the Fundamental Lemma. For 
this example the length of initial trajectory and length of trajectories for reconstruction are chosen as $T = 50, L = 22$ it can be checked that for $N=n$ the \eqref{eq:length} is satisfied.
For the input for this example we are using a randomly generated input using MATLAB's \textit{rand} function. Since the input is random there is a high chance for persistency of excitation before
testing the lemma we check the rank of the $H_{N+L}(u_{[0, T-1]})$. We input the noisy input to the system and record the output data, we then create the Hankel matrix $\begin{bmatrix} H_{L}(u_{[0, T-1]}) \\ H_{L}(y_{[0, T-1]})\end{bmatrix}$.
Now we generate a new random input-output trajectory of the system \((\bar{u}_{[0, L-1]}, \bar{y}_{[0, L-1]})\). We now use the Hankel matrix and the new data to find the vector $g$. We impose that
as a least-squares problem since the Hankel matrix is generally not square and the system of equations is underdetermined (fewer rows than columns). The objective for a minimization problem is the following:
\begin{equation}
    \mbox{Find} \ g\in\mathbb{R}^{T-L-1} \ \mbox{that minimizes} \norm{\begin{bmatrix} H_{L}(u_{[0, T-1]}) \\ H_{L}(y_{[0, T-1]})\end{bmatrix}g - \begin{bmatrix} \bar{u}_{[0, L-1]} \\ \bar{y}_{[0, L-1]} \end{bmatrix}}_{2}^{2}
    \label{eq:gmin}
\end{equation}

In MATLAB we solve the following problem using the \textit{lsqminnorm} function, which uses \textit{complete orthogonal decomposition (COD)} rather than \textit{singular value decomposition (SVD)} to compute $g$, however, for our example
both SVD and COD should yield similar results. Now the Lemma states that $g$ should make that norm of the difference equal to zero. That means the $g$ is an exact solution, not an approximation. Computing the norm for the calculated g we
get result in MATLAB:
\begin{equation}
\norm{\begin{bmatrix} H_{L}(u_{[0, T-1]}) \\ H_{L}(y_{[0, T-1]})\end{bmatrix}g - \begin{bmatrix} \bar{u}_{[0, L-1]} \\ \bar{y}_{[0, L-1]} \end{bmatrix}} \leq 10^{-10}
\label{eq:norm}
\end{equation}

\clearpage

The norm is not exactly 0 but it is really close, the small error arises from truncation of real numbers due to floating point arithmetic, so we can assume the result is valid. Our reconstructed trajectory is found in the image
of the Hankel matrix created from recorded data.

\section{Mechanical MIMO System}
\label{sec:mimo}

In this example we will demonstrate the Lemma on a concrete mechanical system. We will show, like in the last example, that we can reconstruct a trajectory from previously recorded data.

\begin{figure}[ht]
    \centering
    \input{figures/double_spring_mass_damper.tex}
    \caption{Double spring--mass--damper system with force input acting on second mass.}
    \label{fig:double-spring-mass-damper}
\end{figure}

The states of the system are positions and velocities of each mass, the input is a force on the second mass. We chose the output to be a vector of positions of both mass thus this is a MIMO system. Using mechanical equilibrium equations we can get a system of coupled linear differential equations
which can be represented as a continuous state space LTI system like this:
\begin{align*}
    \dot{x}(t) &= Ax(t) + Bu(t) \\
    y(t) &= Cx(t) + Du(t)
\end{align*}

Where (for this concrete example: $m_1 = 1.0kg,\ m_2 = 0.8kg,\ k_1=120Nm^{-1},\ k_2=180Nm^{-1},\ c_1=1.5Nsm^{-1},\ c_2=2.0Nsm^{-1}$):

\begin{alignat}{2}
    \label{eq:dsmd-abcd}
    A &= \begin{bmatrix} 
        0 & 1 & 0 & 0 \\ -\frac{k_1+k_2}{m_1} & -\frac{c_1+c_2}{m_1} & \frac{k_2}{m_1} & \frac{c_2}{m_1} \\ 
        0 & 0 & 0 & 1 \\ \frac{k_2}{m_2} & \frac{c_2}{m_2} & -\frac{k_2}{m_2} & -\frac{c_2}{m_2}
    \end{bmatrix} \ \ & B &= \begin{bmatrix}0 \\ 0 \\ 0 \\ \frac{1}{m_2}\end{bmatrix} \\
    C &= \begin{bmatrix} 1 & 0 & 0 & 0 \\ 0 & 0 & 1 & 0\end{bmatrix}  & D &= \begin{bmatrix} 0 \\ 0 \end{bmatrix}
\end{alignat}

This is a continuos state-space model but we need a discrete model so we need to discretize this. There are many ways of discretizing the continuous model, we will derive the discrete version matrices
$A_d, B_d, C_d, D_d$ using zero order hold (ZOH). Assume sampling period $T_s$, we chose $T_s = 10^{-2}s$. Now we will begin from the solution of the general continuous state space system:

\[x(t) = e^{A(t-t_0)}x(t_0) + \int_{t_0}^{t}e^{A(t-\tau)}Bu(\tau)d\tau\]

\clearpage

Let \(k \in \mathbb{Z}_{\geq 0}\) and \(t_0 = kT_s, \ t = (k+1)T_s\). Define \(x_k:=x(kT_s), \ u_k:=u(kT_s)\). Now we have:
\[x_{k+1} = e^{AT_s}x_k + \int_{kT_s}^{(k+1)T_s}e^{A[(k+1)T_s - \tau]}Bu(\tau)d\tau\]

Now assume \(u(\tau) = u(kT_s) = u_k = const. \ \forall \tau \in (kT_s, (k+1)T_s), \ \forall k \in \mathbb{Z}_{\geq 0}\). This is the ZOH, we assume the input is constant between sampling. After a change of variables
for integration we arrive at this result since B and $u_k$ are constants they can go outside the integral. 
\begin{align*}
    x_{k+1} &= e^{AT_s}x_k + \left(\int_{0}^{T_s}e^{Az}dz\right)Bu_k \\
    y_k &= Cx_k + Du_k
\end{align*}

We can now see that this is essentially \eqref{eq:lti} but where:
\[A_d = e^{AT_s}, \ B_d = \left(\int_{0}^{T_s}e^{Az}dz\right)B, \ C_d = C, \ D_d = D\]

This was a general derivation, in MATLAB we use the function \textit{c2d} to convert the system from continuos to discrete time. Now we can see that $n=4, \ m=1, \ p=2$. The initial trajectory we recorded
is 30 seconds long, in MATLAB this was converted to number of samples with \textit{T = round(30/Ts)}. The length of reconstructed trajectories $L$ is 12 seconds. We set $N=n$ because the systems has four 
energy tanks. For the input, instead of a random sequence of real numbers between 0 and 1, we use a pseudo-random binary sequence (PRBS) - a binary signal which jumps randomly between +1 and -1, we multiply
that signal with a positive real number (amplitude) and feed it through a low-pass filter to make it a bit smoother and more realistic. This choice of T, L and N satisfy the \eqref{eq:length}.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.8\textwidth]{figures/PRBS.pdf}
    \caption{Low-pass filtered PRBS input signal}
    \label{fig:prbs}
\end{figure}

Since this type of input data has enough "randomnes" it is a good choice for a persistently exciting input. And after testing the rank condition
for PE it was always satisfied. 

\clearpage

Now, as before, we input that PE signal to the system and place the recorded trajectories into the according Hankel matrix. We will test the Lemma on two inputs, first will be a step
function and second will be a new PRBS signal. We simulate the output trajectories using the system model and solve for vector g like in \eqref{eq:gmin}.

\begin{figure}[ht]
    \centering
    \begin{subfigure}{\textwidth}
        \centering
        \includegraphics[width=0.95\textwidth]{figures/step.pdf}
        \caption{Output trajectories for a unit step input}
        \label{fig:step-out}
    \end{subfigure}

    \begin{subfigure}{\textwidth}
        \centering
        \includegraphics[width=0.95\textwidth]{figures/prbs_out.pdf}
        \caption{Output trajectories for a PRBS input}
        \label{fig:prbs-out}
    \end{subfigure}
    \caption{Comparison of original and reconstructed trajectories of the system}
\end{figure}

It can be seen that trajectories line-up, this is confirmed numerically with \eqref{eq:norm} seeing that for both trajectories we have the norm $\leq 10^{-10}$.
Thus, the Lemma holds for this example.

\clearpage

\chapter{Data driven simulation of LTI systems}
\label{chp:dd-sim}
In the last chapter we used the FL to demonstrate that any length $L$ trajectory of a linear system can be found
in the image of a Hankel matrix that is built using a previous trajectory with a PE input. This is really interesting, however,
there are no direct use cases when it comes to control. In this chapter we will build up on the FL and use it to
simulate a future of a given trajectory using the same Hankel matrix.

\section{Problem statement and solution}
Take the system \eqref{eq:lti} and assume (A, B) is controllable. Let $L_{ini} \in \mathbb{Z}_{\geq 0}$ be the past length of the trajectory we want to simulate and $L_{ref} \in \mathbb{Z}_{\geq 0}$ 
be the length of the future part. Let $L:=L_{ini} + L_{ref}$. Assume the following are given:
\begin{itemize}
    \item Upper bound on state dimension $N \geq n$ 
    \item Restricted input-output trajectory \((u_{[0, T-1]}, y_{[0, T-1]})\), where $T$ satisfies \eqref{eq:length}
    \item An initial restricted input-output trajectory \((\bar{u}_{[0, L_{ini}-1]}, \bar{y}_{[0, L_{ini}-1]})\)
    \item Reference input \(\bar{u}_{[L_{ini}, L-1]}\)
\end{itemize}

We want to find the output \(\bar{y}_{[L_{ini}, L-1]}\) such that \((\bar{u}_{[0, L-1]}, \bar{y}_{[0, L-1]})\) is a restricted input-output trajectory of \eqref{eq:lti}. So based
on recorded data, previous history of a trajectory and future input we want to find the future output part of that trajectory. We can split the Hankel matrices of recorded data like this:
\begin{equation}
    \begin{bmatrix} H_{L}(u_{[0, T-1]}) \\ H_{L}(y_{[0, T-1]})\end{bmatrix} = \begin{bmatrix}U_p \\ U_f \\ Y_p \\ Y_f\end{bmatrix}
    \label{eq:uy-pf}
\end{equation}

Where $U_p$ and $U_f$ have $mL_{ini}$ and $mL_{ref}$ and $Y_p$ and $Y_f$ have $pL_{ini}$ and $pL_{ref}$ rows, respectively. Now we will formulate a solution of the problem like in  
[TODO: ref DBCBook].

\begin{theorem}
    Consider a system \eqref{eq:lti} with (A, B) controllable and PE input $u_{[0, T-1]}$ of order $N+L$, assume restricted input output trajectory \((\bar{u}_{[0, L_{ini}-1]}, \bar{y}_{[0, L_{ini}-1]})\)
    and reference input \(\bar{u}_{[L_{ini}, L-1]}\). Then the following statements hold:
    \begin{enumerate}
        \item[(i)] There exists $g\in\mathbb{R}^{T-L+1}$ such that: \[\begin{bmatrix}U_p \\ Y_p \\ U_f\end{bmatrix}g = 
                \begin{bmatrix} \bar{u}_{[0, L_{ini} -1]} \\ \bar{y}_{[0, L_{ini} -1]} \\ \bar{u}_{[L_{ini}, L-1]}\end{bmatrix}\]
        \item[(ii)] If $g$ is a solution to (i) then define the output \(\bar{y}_{[L_{ini}, L-1]} = Y_{f}g\). Such defined output
                    makes \((\bar{u}_{[0, L-1]}, \bar{y}_{[0, L-1]})\) a restricted input-output trajectory of \eqref{eq:lti}
        \item[(iii)] If $L_{ini} \geq \ell$ than such output in (ii) is the only one that makes \((\bar{u}_{[0, L-1]}, \bar{y}_{[0, L-1]})\)
                    a restricted input-output trajectory, i.e. it is unique.
    \end{enumerate}
    \label{thm:sim}
\end{theorem}

\clearpage

\section{Simulating LTI system from data}
\label{sec:lti-sim}

This theorem gives us the method on how to simulate the future trajectory of the system. We will test this result on examples. Firstly, we will test try to simulate future output trajectory
for the LTI MIMO example from the previous chapter (\ref{fig:double-spring-mass-damper}). We simulated the system with a PRBS input for 50 seconds ($T_s = 0.01s \ T = 5000$) and recorded the trajectory 
\((u_{[0, T-1]}, y_{[0, T-1]})\). We chose the following lengths of past and future part of to be simulated trajectory:
\[L_{ini} = 500, \ L_{ref} = 1000, \ L = L_{ini} + L_{ref} = 1500\]

We populated the $H_{L}(u_{[0, T-1]})$ and $H_{L}(y_{[0, T-1]})$ and split them into $(U_p, Y_p, U_f, Y_f)$ parts. Result from (\ref{thm:sim}, (i)) tells us $g$ exists, we determine it using the modified
least-squares problem from the last chapter \eqref{eq:gmin}:
\begin{equation}
    \mbox{Find} \ g\in\mathbb{R}^{T-L-1} \ \mbox{that minimizes} \norm{\begin{bmatrix}U_p \\ Y_p \\ U_f\end{bmatrix}g - \begin{bmatrix} \bar{u}_{[0, L_{ini} -1]} \\ \bar{y}_{[0, L_{ini} -1]} \\ \bar{u}_{[L_{ini}, L-1]}\end{bmatrix}}_{2}^{2}
    \label{eq:sim-gmin}
\end{equation}

Solving with MATLAB's \textit{lsqminnorm} we obtain the vector $g$. Using $g$ we calculate the future output trajectory \(\bar{y}_{[L_{ini}, L-1]} = Y_{f}g\). By theorem (\ref{thm:sim}) this
output part is unique since $L_{ini} \geq \ell = 2 $ (for the parameters given in \eqref{eq:dsmd-abcd}), generally, the lag is unknown, but if we take large enough $L_{ini}$ we can be sure that it is larger.
Now we will compare the data based future output trajectories with one calculated using the system model - the first $L_{ini}$ points using the model. For the input signal we used a multi-sine input like this:
\[u(k) = 10\sin(2\pi kT_s) + \frac{1}{3}\sin(2\pi 3 kT_s), \ k \in \{0, 1, \dots, L-1\}\]

The simulation output is:

\begin{figure}[htbp]
    \centering
    \includegraphics[width=1\textwidth]{figures/dd_sim_linear.pdf}
    \caption{Data-based vs. model-based future trajectory simulation}
    \label{fig:dd-lin}
\end{figure}

\newpage

We can see that the data-based simulated trajectories perfectly match the model-based ones. Numerically, like before, we get the following confirmation that the future output trajectories coincide:
\[\norm{\bar{y}_{[L_{ini}, L-1]} - \bar{y}_{[L_{ini}, L-1]}^{\ model}} \leq 10^{-10}\]

\section{Simulating a nonlinear system around equilibrium}
In the last section we showed that it is possible to simulate a LTI system directly from recorded data without the model. For nonlinear systems, a classical approach is to linearize the 
dynamics around a stationary point around which the linearized model is mostly valid. We want to gather the data around the stationary (equilibrium) point of the system and apply the data-based
simulation of the future trajectory. For this example we will take a classical torque driven pendulum: 

\begin{figure}[htbp]
    \centering
    \input{figures/pendulum.tex}
    \caption{Torque driven simple pendulum}
    \label{fig:pendulum}
\end{figure}

Parameters for our pendulum are:
\[m = 0.2\ kg, \ l = 0.5 \ m, \ b = 0.05\ Nms, \ g = 9.81 \ \frac{m}{s^2}\]

Assuming damping is proportional to angular velocity with proportionality constant $b$, we write the state space dynamics which can be derived with various approaches from classical mechanics, state vector is \(x = (\theta, \ \dot{\theta})^T\):

\begin{equation}
    \dot{x} = \dfrac{d}{dt} \begin{bmatrix} \theta \\ \dot{\theta}\end{bmatrix} = \begin{bmatrix} \dot{\theta} \\ \dfrac{-b}{ml^2}\dot{\theta} - \dfrac{g}{l}\sin(\theta) + \dfrac{u}{ml^2}\end{bmatrix} = f(x, u)
    \label{eq:pend-dyn}
\end{equation}

Since this is in continous state space forumlation we need to derive a discrete time version of it. Using sampling period \(T_s = 0.01s\) and assuming
ZOH on the input $u(t)$ (see \ref{sec:mimo}) we discretize \eqref{eq:pend-dyn} using the Runge-Kutta 4 method like this:
\begin{equation}
    x_{k+1} = x_k + \frac{T_s}{6}(k_1 + 2k_2 + 2k_3 + k_4) 
\end{equation}
Where, 
\begin{align*}
    k_1 &= f(x_k, u_k) \\
    k_2 &= f(x_k + \frac{T_s}{2}k_1, u_k) \\
    k_3 &= f(x_k + \frac{T_s}{2}k_2, u_k) \\
    k_4 &= f(x_k + T_sk_3, u_k)
\end{align*}

Assuming initial state \(x_0 = (0, 0)^T\) and that the output is the full state of the system ($y_k = x_k$) we supply a PRBS input for 50 seconds to the system and record the output. We configured the amplitude of the PRBS such that angle $\theta$ is always $\leq \frac{\pi}{18}$ (10 degrees).

\newpage

Since $\theta$ is small enough and the model is approximately linear around 0 we will try to simulte the future trajectory of the system using the FL.
We chose the following trajectory lengths \(L_{ini} = 50, \ L_{ref} = 600 \implies L = 650\), we assumed $N=4$. Since a PRBS input is PE of order $N + L$  we can repeat the procedure in \ref{sec:lti-sim}.
We give a zero input and set the initial state to $\left(\dfrac{\pi}{10}, 0\right)^T$ for the simulating trajectory - autonomous system.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=1\textwidth]{figures/pend_1.pdf}
    \caption{Pendulum trajectory simulation - data-based vs. model}
    \label{fig:pend-1}
\end{figure}

If we now calculate the norm of the difference between the two output trajectories we get:
\[\norm{\bar{y}_{[L_{ini}, L-1]}^{\ data} - \bar{y}_{[L_{ini}, L-1]}^{\ model}} \sim 10^{-2}\]

Our trajectories do not coincide exactly, but that is to be expected because the model is nonlinear, since the error is small we can say that
data-based simulation works well for small angles. Now we will try simulating with a larger angle and we changed $L_{ini} = 10$. We will also compare
a linearized model of \eqref{eq:pend-dyn} which uses \(\sin(\theta) \approx \theta\) linearization. For initial angle of $\frac{\pi}{3}$ and zero input we get
the following results (for a specific simulation):

\begin{align}
    \norm{\bar{y}_{[L_{ini}, L-1]}^{\ data} - \bar{y}_{[L_{ini}, L-1]}^{\ model}} &\approx 0.428618 \\
    \norm{\bar{y}_{[L_{ini}, L-1]}^{\ lin. model} - \bar{y}_{[L_{ini}, L-1]}^{\ model}} &\approx 1.446429
    \label{eq:lin-model-err}
\end{align}

We can see that the error for the data-based simulation is quite smaller, which can be seen on the following plot:

\newpage

\begin{figure}[htbp]
    \centering
    \includegraphics[width=1.0\textwidth]{figures/pend_sim_comp.pdf}
    \caption{Pendulum trajectory simulation - large angles; - data-based vs. model vs. linearized model}
    \label{fig:pend-2}
\end{figure}

We can see that the data-based simulation is closer to the nonlinear model one and it is considerably more accurate for larger angles.
However, this MATLAB script inputs a new PRBS to the system for the data every time the script is run, randomness of that signal implies that the result from above is not
general, in fact, we sometimes got that the linearized model produces a lower error than the data-based. In practice, this could be fixed by recording multiple data sets
with PRBS and keeping the one which makes the data-based simulation the best. We ran the MATLAB script 1000 times and recorded the norm of the difference for
each one, the linearized model error \eqref{eq:lin-model-err} is constant because it does not depend on a PRBS input. These are the results:

\begin{align*}
    \norm{\bar{y}_{[L_{ini}, L-1]}^{\ data} - \bar{y}_{[L_{ini}, L-1]}^{\ model}}_{average} &= 0.685848 \\
    \norm{\bar{y}_{[L_{ini}, L-1]}^{\ data} - \bar{y}_{[L_{ini}, L-1]}^{\ model}}_{max} &=  3.47226 \\
    \norm{\bar{y}_{[L_{ini}, L-1]}^{\ data} - \bar{y}_{[L_{ini}, L-1]}^{\ model}}_{min} &= 0.238929 
\end{align*}

This proves that with data-based simulation we can get a better result than a linearized model, however, if we do not supply a "good" PE input we can get worse results. 

\vspace{1em}

In this chapter we demonstrated that it is possible to simulate future trajectories of LTI systems directly from recorded data and knowledge about the input. This
hints at the following chapter which uses this approach to implement predictive control of such systems entirely from input-output data.

\newpage

\chapter{Data-Enabled Predictive Control (DeePC)}

\section{Introduction}

Classical control theory almost always uses a model of the system to synthesise a controller. Usually that model is obtain by system identification from recorded data, but when the model
is too complex to be easily identified or approximated from first principles people started developing control methods based solely on data.
During the last decade data availability and processing power have been growing sharply thus data-driven control methods have started do develop intensively, there has been a lot of progress in the machine learning approach. In this chapter we will build up on
results from previous chapters to create a controller that uses only input/output data, bypassing a model. A needed prerequisite for our data-based controller is model predictive control (MPC),
an optimal control algorithm which finds optimal input to the system on a finite and recending horizon whose data is constrained with the model and other (physical) limitations.
The DeePC algorithm, essentially, does the same thing as MPC - same optimization problem, just instead of the model it uses previously recorded data for the prediction part. This 
algorithm relies on the Fundamental Lemma since it's result states that it is possible to predict future trajectory of a linear system given input data and recorded data. First formulation (and naming)
of DeePC was given by [TODO: insert reference dorfler deepc]. A good overview on data-based control that stems from the behavioural framework and the FL can be found at [TODO: special issue control systems mag ieee] and [annual reviews in control 100914]

\begin{figure}[htbp]
    \centering
    \input{figures/control_diagram.tex}
    \caption{Different control design approaches from data}
    \label{fig:data-based-vs-model}
\end{figure}

\begin{figure}[htbp]
    \centering
s
    \resizebox{0.7\textwidth}{!}{
    \input{figures/predictive_control.tex}
    }
    \caption{General predictive control illustration}
    \label{fig:pred-control}
\end{figure}

\newpage

\section{Model predictive control}
As mentioned before, MPC is a control algorithm which uses optimization with system model and limitations as constraints to find optimal control input over a finite, receding horizon.
At each time step future trajectory of the system is predicted using the model and optimal sequence of control input is computed taking constraints on inputs and states of the system. Typically, only the first
input of the optimal input sequence is applied after which the procedure is repeated with new measurements. We can say that, other than MPC, we do not have another control solution for multivariable systems with constraints 
on inputs and outputs with stability guarantees. In this section we will formulate classical MPC for LTI systems which is needed for later understanding of DeePC.

\vspace{1em}

Given a LTI system \eqref{eq:lti} the classical finite-horizon MPC optimization problem at time step $t$ can be written as:

\begin{equation}
    \begin{aligned}
        \min_{x,y,u} \quad  &\sum_{j=0}^{N-1} \left(\norm{y_j - r_{t+j}}^{2}_{Q} + \norm{u_j}^{2}_{R}\right) \\
        \text{subject to:} \quad &x_0 = \hat{x}(t), \\
        &x_{j+1} = Ax_j + Bu_j, \quad j=0,\dots,N-1; \\
        &y_j = Cx_j + Du_j, \quad j=0,\dots,N-1; \\
        &y_j \in \mathcal{Y}, \quad j=0,\dots,N-1; \\
        &u_j \in \mathcal{U}, \quad j=0,\dots,N-1; \\
    \end{aligned}
    \label{eq:mpc}
\end{equation}


Where: \(\norm{z}^{2}_P := z^{T}Pz\), for a symmetric positive semidefinite matrix P (notation $P \succeq 0$). Matrix \(Q\in M_{p\times p}(\mathbb{R}), \ Q \succeq 0\) is the 
output cost matrix and \(R\in M_{m\times m}(\mathbb{R}), \ R \succeq 0\) is the input cost matrix. Sets \(\mathcal{U} \subseteq \mathbb{R}^m, \mathcal{Y} \subseteq \mathbb{R}^p\) are input and 
output constraints set, respectively. Sequence \((r_0, r_1, ...) \) is the reference trajectory which we want to track, $N\in\mathbb{Z}_{>0}$ is the prediction horizon length. We use estimated state $\hat{x}$ instead of the true state because, generally,
the output is not the whole state i.e. we cannot measure the whole state thus we need to estimate it. This MPC optimization problem is usually a convex quadratic program (QP).
Now we will lay out the MPC algorithm for reference tracking.

\begin{algorithm}
\caption{Model predictive control (MPC)}
\begin{algorithmic}
\Require System model $(A, B, C, D)$; reference trajectory $r$; past input/output data $(u, y)$; contraints sets $\mathcal{U}, \mathcal{Y}$; cost matrices $Q, R$;

\For{$t = 0, 1, ...$}
    \State[1] Estimate $\hat{x}(t)$ from previous input/output data
    \State[2] Solve \eqref{eq:mpc} for $u^{*} = (u^{*}_0, u^{*}_1, ..., u^{*}_{N-1})$ 
    \State[3] Apply input $u(t) = u^{*}_0$ 
    \State[4] Update past input/output data with new inputs/measurements
\EndFor
\end{algorithmic}
\end{algorithm}

This formulation of MPC assumes stationary state inputs are 0, also terminal cost is omitted because when we transfer to DeePC we do not know the full state of the system. 
It can be shown, under typical assumptions, that MPC algorithm is recursively feasable and stabilizing. This algorithm can produce excellent results
if the system model is precise and if it is possible to estimate the initial state $\hat{x}(t)$.

\newpage

\section{DeePC for LTI systems}
In this section we will formulate predictive control based on data. We will use the same simulation method for prediction from Chapter \ref{chp:dd-sim}.
The following are required for the DeePC algorithm:
\begin{itemize}
    \item[(i)] An upper bound on state dimension $N$
    \item[(ii)] Past/future trajectory lengths $L_{ini}$ and $L_{ref}$, with $L_{ini} \geq \ell$; $L:=L_{ini}+L_{ref}$  
    \item[(iii)] Recorded restricted input-output trajectory \((u_{[0, T-1]}, y_{[0, T-1]})\), where $u_{[0, T-1]}$ is PE of order N+L and $T \geq (m+1)(N+L) - 1$, $m$ is the number of inputs
    \item[(iv)] Initial (past) trajectory \((\bar{u}_{ini}, \bar{y}_{ini}) = (\bar{u}_{[0, L_{ini}-1]}, \bar{y}_{[0, L_{ini}-1]})\) 
    \item[(v)] Reference trajectory $(r_0, r_1, ...) \in (\mathbb{R}^p)^{\mathbb{Z}_{\geq 0}}$ 
    \item[(vi)] Input and output cost matrices $Q \succeq 0,\ R \succeq 0$
    \item[(vii)] Constraits sets $\mathcal{U}, \ \mathcal{Y}$ 
\end{itemize}

Requirement (ii) assuemes initial trajectory length longer than the lag of the system which is unknown, in practice we take long enough initial trajectory. That condition 
is essentially equivalent to having long past input-output data for initial state estimation in MPC. Now we define matrices
$U_p, U_f, Y_p, Y_f$ like in \eqref{eq:uy-pf}. Conditions above allow for application of Theorem \ref{thm:sim}. We now formulate the DeePC optimization problem at time step $t$:

\begin{equation}
    \begin{aligned}
        \min_{g,y,u} \quad  &\sum_{j=0}^{L_{ref}-1} \left(\norm{\bar{y}_j - r_{t+j}}^{2}_{Q} + \norm{\bar{u}_j}^{2}_{R}\right) \\
        \text{subject to:} \quad & \begin{bmatrix} U_p \\ Y_P \\ U_f \\ Y_f\end{bmatrix}g = \begin{bmatrix} \bar{u}_{ini} \\ \bar{y}_{ini} \\ \bar{u} \\ \bar{y}\end{bmatrix}\\ 
        &\bar{y}_j \in \mathcal{Y}, \quad j=0,\dots,L_{ref}-1; \\
        &\bar{u}_j \in \mathcal{U}, \quad j=0,\dots,L_{ref}-1; \\
    \end{aligned}
    \label{eq:deepc}
\end{equation}

Where:
\begin{align*}
    \bar{u} &= col(\bar{u}_0, \dots, \bar{u}_{L_{ref} - 1}) \\
    \bar{y} &= col(\bar{y}_0, \dots, \bar{y}_{L_{ref} - 1}) 
\end{align*}

We deviate from previos nomenclature in favor of easier formulation. Initial trajectory $(u_{ini}, y_{ini})$ will be updated with new meausred data such that it keeps the same length thus the oldest
data pair will be removed.

\vspace{1em}

We can see that the DeePC optimization problem is \textit{de facto} a MPC problem but where instead of prediction using the model we use previous input-output data, thus this problem aviods the model
completely. We now formulate the DeePC algorithm:

\newpage

\begin{algorithm}
\caption{Data-Enabled predictive control (DeePC)}
\begin{algorithmic}
\Require Requirements (i) - (vii) listed above

\For{$t = 0, 1, ...$}
    \State[1] Solve \eqref{eq:deepc} for the vector g
    \State[2] Compute optimal input sequence $\bar{u}^{*} = U_{f}g$
    \State[3] Apply the first input $u(t) = \bar{u}^{*}(0)$
    \State[4] Update the initial trajectory $(u_{ini}, y_{ini})$ with new inputs/measurements 
\EndFor
\end{algorithmic}
\end{algorithm}

It can be shown that this DeePC is equivalent to MPC under the assumptions of the Fundamental Lemma, i.e. given the same reference and initial trajectory 
they will give the identical output.

\vspace{1em}

Now, will we try to control the double spring mass damper system \ref{fig:double-spring-mass-damper} with DeePC. We will try to control
the position of mass 2 so we changed  the output equation (\(C = \begin{bmatrix} 0 & 0 & 1 & 0 \end{bmatrix}, \ D = 0\)) to only give position of mass 2 to reduce computational cost.
We put an input constraint \(-100 \leq u(t) \leq 100\). We chose the following parameters $T = 100, \ L_{ini} = 5, \ L_{ref} = 25, \ Q = 10000, \ R = 0.001$ - changing them we can
tune the behaviour of our controller. Initial trajectory is set to 0.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=1.0\textwidth]{figures/deepc_step_y.pdf}
    \caption{Mass 2 position controlled with DeePC}
    \label{fig:deepc-step-y}
\end{figure}

We can see that we were able to reach tracking of the given trajectory, however, it could be tuned for a better result, but as a proof of concept it is sufficient. Now lets display
the limited force input.

\newpage

\begin{figure}[htbp]
    \centering
    \includegraphics[width=1.0\textwidth]{figures/deepc_step_u.pdf}
    \caption{Force input for mass 2 position control with DeePC}
    \label{fig:deepc-step-u}
\end{figure}

We can see that the limit on the input is working. Now lets try limiting the position $y \leq 1$ to eliminate overshoot. 

\begin{figure}[htbp]
    \centering
    \includegraphics[width=1.0\textwidth]{figures/deepc_step_ylim.pdf}
    \caption{Position of mass 2 and force input with DeePC - y limit}
    \label{fig:deepc-step-ylim}
\end{figure}

Tuning the paramters gave us this result, which can be considered relatively good, we got a short settling time with no overshoot. For these simulations we used MATLAB with YALMIP
toolbox and MOSEK solver.

\vspace{1em}

In simulation where systems are completely linear and there is zero noise we get a good result, however, when we consider realistic systems with noise and nonlinearites this 
DeePC performs poorly - it is not robust. To improve on robustness we implement regularizations to our optimization problem.

\newpage


\section{Regularized DeePC}
The paper [todo: deepc] introduced robustifications to the DeePC algorithm \eqref{eq:deepc} to make it applicable to realistic systems. We will lay out their result and simulate it.
The Fundamental lemma is an result which completely falls apart when we introduce measurement noise (or nonlinearity) to our system since there is a high probability that the image of Hankel matrix of input-output
data spans the whole space instead of the aforementioned subspace (the behaviour). There is an approach where we can maybe remove the noise from recorded data with SVD or similar techniques, that is good for recorded data but for DeePC where
we use real-time data it is not that simple. Now we will formulate regularized DeePC optimization problem:

\begin{equation}
    \begin{aligned}
        \min_{g,y,u, \sigma_y} \quad  &\sum_{j=0}^{L_{ref}-1} \left(\norm{\bar{y}_j - r_{t+j}}^{2}_{Q} + \norm{\bar{u}_j}^{2}_{R}\right) \\
        &+ \lambda_g\norm{g}_2^{2}\ + \lambda_y\norm{\sigma_y}_2^{2}\\ 
        \text{subject to:} \quad & \begin{bmatrix} U_p \\ Y_P \\ U_f \\ Y_f\end{bmatrix}g = \begin{bmatrix} \bar{u}_{ini} \\ \bar{y}_{ini} + \sigma_y \\ \bar{u} \\ \bar{y}\end{bmatrix}\\ 
        &\bar{y}_j \in \mathcal{Y}, \quad j=0,\dots,L_{ref}-1; \\
        &\bar{u}_j \in \mathcal{U}, \quad j=0,\dots,L_{ref}-1; \\
    \end{aligned}
    \label{eq:deepc-reg}
\end{equation}

The hard equality constraint in the original problem can cause problems when noise is added, so we impose a soft constraint with slack variable $\sigma_y \in \mathbb{R}^{pL_{ini}}$
to ensure the feasibility of the constraint. We penalize the 2-norm squared of the slack variable with the weight $\lambda_y \in \mathbb{R}$, choosing sufficiently large $\lambda_y$ "ensures" that $\sigma_y \neq 0$ only if the constraint
is infeasible i.e. that data is inconsistent. Two-norm squared regularization on $g$ with weight $\lambda_g \in \mathbb{R}$ is motivated by previous robust optimization problems, more detail in [ref]. 
In the original paper [ref] 1-norm regularization was used, but in a more recent paper [ref] it was updated to 2-norm regularization.

\subsection{Noisy measurements LTI example}

We test the regularized DeePC on \ref{fig:double-spring-mass-damper}. Since this example requires a non-zero input for a non-zero steady state we replace $\norm{\bar{u}_j}^{2}_{R}$ with
$\norm{\bar{u}_j - u_s}^{2}_{R}$ in \eqref{eq:deepc-reg}, where $u_s \in \mathbb{R}^m$ is the steady state input. 
This steady state input is, generally, not necessary for DeePC and it can be calculated from data. 
We calculate the steady state input from the model by solving for $u_s$:
\begin{equation}
\begin{aligned}
    (I-A_d)x_s - B_du_s = 0 \\
    Cx_s + Du_s = y_s
\end{aligned}
\label{eq:steady}
\end{equation}

Where $y_s$ is the steady state output, for our example where $y_s = 1\ m$ solving \eqref{eq:steady} yields $u_s \approx 71.94\ N$.
We introduce Gaussian measurement noise to the system model like this:
\begin{equation}
    y_k = Cx_k + Du_k + v_k, \ v_k \sim \mathcal{N}(0, \sigma^2)
    \label{eq:noise}
\end{equation}

For this example we took $\sigma = 0.01m$.
Now we run regularized DeePC algorithm to track the steady state.

\newpage

\begin{figure}[htbp]
    \centering
    \includegraphics[width=1.0\textwidth]{figures/deepc_step_robust.pdf}
    \caption{Regularized DeePC vs. DeePC; Noisy measurement LTI system}
    \label{fig:deepc-step-reg}
\end{figure}

Tuning the parameters of \eqref{eq:deepc-reg} gave us a really good result for regularized DeePC and also we can see that DeePC without regularizations
performs poorly with the same parameters and data.

\subsection{Nonlinear system example}

\newpage


% \chapter{Conclusion}
% Summarize the main contributions, limitations, and possible directions for future work.

    \chapter{Conclusion}

\appendix
\chapter{Additional Material}

\begin{thebibliography}{9}
\addcontentsline{toc}{chapter}{Bibliography}

\bibitem{example}
Author Name.
\textit{Title of the Book or Article}.
Publisher or Journal, year.

\end{thebibliography}



\end{document}
